services:
  cloudflare:
    image: cloudflare/cloudflared:latest
    restart: always
    networks:
      - internal
    command: tunnel --no-autoupdate run --token ${CF_TUNNEL_TOKEN}
    env_file:
      - .env

networks:
  internal:
    external: true
    name: internal